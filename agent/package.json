{
  "name": "@trustflow/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous subscriber agent — funds, monitors and cancels its own TrustFlow streams",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.48.1"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
